# SpaceDB — a local-first, CRDT-native, mesh-replicated database with no data center.
# Open source, dual-licensed MIT OR Apache-2.0. Embed and run it locally for free; distribute
# across the mesh with MATA disco. Publish order is derived from the crate graph, not this list.
# Generated by release/release.sh — do not edit by hand; the mono is canonical.
[workspace]
resolver = "2"
members = [
    "spacedb-store",
    "spacedb-crdt",
    "spacedb-replica",
    "spacedb-durability",
    "spacedb-access",
    "spacedb-query",
    "spacedb-vector",
    "spacedb-consistency",
    "spacedb-meter",
    "spacedb-sdk",
    "spacedb-console",
    "spacedb-sim",
]

[workspace.dependencies]
spacedb-store = { path = "spacedb-store", version = "0.5.2" }
spacedb-crdt = { path = "spacedb-crdt", version = "0.5.2" }
spacedb-replica = { path = "spacedb-replica", version = "0.5.2" }
spacedb-durability = { path = "spacedb-durability", version = "0.5.2" }
spacedb-access = { path = "spacedb-access", version = "0.5.2" }
spacedb-query = { path = "spacedb-query", version = "0.5.2" }
spacedb-vector = { path = "spacedb-vector", version = "0.5.2" }
spacedb-consistency = { path = "spacedb-consistency", version = "0.5.2" }
spacedb-meter = { path = "spacedb-meter", version = "0.5.2" }
spacedb-sdk = { path = "spacedb-sdk", version = "0.5.2" }
spacedb-console = { path = "spacedb-console", version = "0.5.2" }
spacedb-sim = { path = "spacedb-sim", version = "0.5.2" }
